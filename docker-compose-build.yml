services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    image: helian-agent-backend:latest

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    image: helian-agent-frontend:latest
